// This file has been deprecated - encryption removed from the system
// All credentials are now stored as plain text for simplicity

export function encrypt(text: string): string {
  // No longer encrypts - just returns the text as-is
  return text
}

export function decrypt(text: string): string {
  // No longer decrypts - just returns the text as-is
  return text
}